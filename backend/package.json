{
  "name": "backend",
  "version": "1.0.0",
  "scripts": {
    "build": "npx tsc",
    "start": "node dist/server.js",
    "dev": "nodemon src/server.ts",
    "// --- INI CLI NYE TINGGAL COPY AJE YE ---": "",
    "docker:up": "docker compose up -d",
    "docker:down": "docker compose down",
    "docker:logs": "docker compose logs -f",
    "db:push": "npx prisma db push",
    "db:studio": "npx prisma studio",
    "db:reset": "docker compose down && docker compose up -d && sleep 5 && npx prisma db push"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "dotenv": "^17.2.3",
    "jsonwebtoken": "^9.0.3"
  },
  "devDependencies": {
    "@types/bcryptjs": "^2.4.6",
    "@types/express": "^5.0.6",
    "@types/jsonwebtoken": "^9.0.10"
  }
}
